# Dev-only dependencies for running the test suite.
# Install: pip install -r requirements-dev.txt
#
# Kept as a standalone file so it doesn't conflict with the in-progress
# agentpay SDK pyproject.toml work. Once that lands, this can move into
# [project.optional-dependencies] dev = [...] in pyproject.toml and this
# file can be deleted.

pytest>=8.0
pytest-asyncio>=0.23
respx>=0.21

# The gateway's own pins, so the suite runs against the versions prod runs.
# Resolving these loosely let CI drift to a fastapi/starlette/slowapi
# combination where slowapi's middleware cannot find the handler behind an
# included router, so the default rate limit never applied and the tests
# that pin it failed only there.
-r requirements.txt
